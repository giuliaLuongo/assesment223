import { PowerConverterPipe } from './power-converter.pipe';

describe('PowerConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new PowerConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
