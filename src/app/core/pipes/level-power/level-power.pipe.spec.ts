import { LevelPowerPipe } from './level-power.pipe';

describe('LevelPowerPipe', () => {
  it('create an instance', () => {
    const pipe = new LevelPowerPipe();
    expect(pipe).toBeTruthy();
  });
});
