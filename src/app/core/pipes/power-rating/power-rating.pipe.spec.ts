import { PowerRatingPipe } from './power-rating.pipe';

describe('PowerRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new PowerRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
